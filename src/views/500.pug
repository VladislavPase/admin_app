block config
    - var pagetitle = 'Error: 500 Unexpected Error'
    - var bodyClass = 'error error_500'
    - var pageName = '500'
    - var name = 'error'
doctype html
html(lang=__.lang, class="no-js")
    head
        block head
            include _includes/head
            block styles
                link(rel='stylesheet', href='assets/css/common.css?v=' + PACKAGE.version)
    body.body(class=bodyClass)
        h1 Error: 500 Unexpected Error
        .error__code
            span 5
            img(src="assets/img/face.svg")
            span 5
        p Возникла ошибка, ваш запрос не может будет закончен.
        p Проверьте URL, или вернитесь 
            a(href="./").link На главную, 
            | или обратите в 
            a(href="mailto:").link Службу поддержки.
        block scripts
            include _includes/scripts
