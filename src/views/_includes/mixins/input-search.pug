mixin inputSearch()
    .fieldset.input-search
        i.icon-search.icon
        input.input(name=attributes.name type="text" id=attributes.id)
        label.label(for=attributes.id)=attributes.label
