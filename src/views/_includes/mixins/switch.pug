mixin switch()
    .switch-item(class=attributes.class)
        label.switch
            input(type="checkbox" name=attributes.name value=attributes.value checked=attributes.checked)
            span.slider
        p.switch__name=attributes.switch_name
        if attributes.helper
            .helper(data-text=attributes.helperText)
                span ?
